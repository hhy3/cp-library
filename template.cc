#include <bits/stdc++.h>
using namespace std;

void solve() {

}

#define MULTIPLE_CASE 0
#define GOOGLE 0
int main() {
  ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#if MULTIPLE_CASE
  int ncase; cin >> ncase;
  for (int i = 1; i <= ncase; ++i) {
#endif
#if GOOGLE
    cout << "Case #" << i << ": ";
#endif
    solve();
#if MULTIPLE_CASE
  }
#endif
}
