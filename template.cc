#include <bits/stdc++.h>

void solve() {}

#define MULTIPLE_CASE 0
#define GOOGLE 0

int main() {
  std::cin.tie(0)->sync_with_stdio(0);
#if MULTIPLE_CASE
  int ncase;
  std::cin >> ncase;
  for (int i = 1; i <= ncase; ++i) {
#endif
#if GOOGLE
    std::cout << "Case #" << i << ": ";
#endif
    solve();
#if MULTIPLE_CASE
  }
#endif
}
